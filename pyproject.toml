[project]
name = "claude-agent-kit-evals"
version = "0.1.0"
description = "Evaluation scripts for the claude-agent-kit plugin"
requires-python = ">=3.11"
dependencies = [
    "langfuse>=2.0.0",
    "anthropic>=0.40.0",
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
